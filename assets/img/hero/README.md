Place hero visual assets in this folder.

Currently referenced by `index.html`:

- `mlp-tip.jpg` (machine-learning potential tip image shown below hero links)

If the file is missing, the site falls back to:

- `assets/img/work-placeholder.svg`
